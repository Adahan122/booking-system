# 🎓 Email-функциональность для системы бронирования аудиторий

## ✨ Что получилось?

Система теперь **автоматически отправляет email-уведомления** студентам:

```
Студент хочет забронировать → Аудитория занята → Встает в очередь
                                                  ↓
                                         📧 Email: "Вы в очереди #2"
                                                  ↓
Преподаватель отменяет бронирование → Система ищет первого в очереди
                                                  ↓
                                         📧 Email: "Очередь дошла!"
```

## 📦 Установка и настройка (5 минут)

### 1. Установите Flask-Mail

```bash
pip install Flask-Mail
# или если у вас новый環境
pip install -r requirements.txt
```

### 2. Получите Google App Password

Если используете Gmail (рекомендуется):

1. Перейдите на https://myaccount.google.com/apppasswords
2. Выберите **Mail** + ваше устройство
3. Google выдаст 16-символный пароль → **скопируйте его**

⚠️ **ВАЖНО**: Пароль содержит 4 группы по 4 символа с пробелами. Это нормально!

### 3. Настройте `.env` файл

Отредактируйте файл `.env` в корне проекта:

```env
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=xxxx xxxx xxxx xxxx
```

Остальные параметры уже настроены для Gmail.

### 4. Протестируйте конфигурацию

```bash
python test_email.py
# Проверяет конфигурацию

python test_email.py --send
# Отправляет тестовое письмо себе на почту
```

### 5. Запустите сервер

```bash
python run.py
```

**Готово!** 🎉 Система начнет отправлять письма.

## 📧 Примеры отправляемых писем

### Email #1 - Уведомление об очереди

**Когда:** Студент пытается забронировать занятую аудиторию

```
From: noreply@booking-system.com
Subject: 📋 Вы добавлены в очередь - Позиция #2

Здравствуйте, Иван!

Аудитория 42 на 05.12.2025 с 10:00 по 12:00 уже занята.
Вы добавлены в очередь ожидания.

Ваша позиция: #2

Когда аудитория освободится, мы вас уведомим!
```

### Email #2 - Уведомление об одобрении

**Когда:** Оригинальное бронирование отменено, студент может забронировать

```
From: noreply@booking-system.com
Subject: ✅ Ваша очередь дошла! Аудитория 42 свободна

Здравствуйте, Иван!

🎉 Отличная новость!

Аудитория 42 на 05.12.2025 с 10:00 по 12:00 теперь свободна!

Пожалуйста, подтвердите бронирование в течение 1 часа.
Иначе это время может быть предложено другому пользователю.
```

## 🔧 Структура новых файлов

| Файл               | Назначение                        |
| ------------------ | --------------------------------- |
| `app/email.py`     | Модуль с функциями отправки email |
| `.env`             | Конфигурация SMTP сервера         |
| `test_email.py`    | Скрипт для тестирования           |
| `requirements.txt` | Обновлен с Flask-Mail             |

## 🔐 Поддерживаемые SMTP серверы

### Gmail (рекомендуется)

```env
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
```

### Яндекс.Почта

```env
MAIL_SERVER=smtp.yandex.ru
MAIL_PORT=465
MAIL_USE_TLS=False
```

### Outlook/Hotmail

```env
MAIL_SERVER=smtp-mail.outlook.com
MAIL_PORT=587
MAIL_USE_TLS=True
```

Полные примеры смотрите в **EMAIL_SETUP_GUIDE.md**

## 🚨 Решение проблем

### "Письма не приходят"

1. Проверьте папку SPAM/Яндекс.Фильтр
2. Запустите `python test_email.py --send` для тестирования
3. Проверьте логи консоли на ошибки

### "Authentication failed"

1. Убедитесь, что используете 16-символный App Password, а не обычный пароль
2. Проверьте правильность MAIL_USERNAME
3. Пароль может содержать пробелы - это нормально

### "Connection timeout"

1. Проверьте интернет соединение
2. Убедитесь, что MAIL_SERVER и MAIL_PORT правильные
3. Может быть блокирован firewall - используйте VPN

### "SSL certificate error"

Попробуйте установить сертификаты:

```bash
# macOS
/Applications/Python\ 3.11/Install\ Certificates.command

# Windows - в PowerShell от администратора
pip install certifi
```

## 📚 Документация

- **EMAIL_SETUP_GUIDE.md** - полная инструкция по настройке
- **QUICK_EMAIL_SETUP.txt** - 5-минутный старт
- **EMAIL_FLOW_DIAGRAM.md** - архитектура и диаграммы
- **EMAIL_IMPLEMENTATION_SUMMARY.md** - что было реализовано

## 🔍 Как это работает?

### Код в routes.py - Пример

Когда студент встает в очередь:

```python
# Создаем запись в очереди
new_queue_entry = BookingQueue(...)
db.session.add(new_queue_entry)
db.session.commit()

# Отправляем email (в отдельном потоке!)
send_queue_notification_email(
    user_email=current_user.email,
    username=current_user.username,
    classroom_number=42,
    position=2,
    booking_date='05.12.2025',
    start_time='10:00',
    end_time='12:00'
)
```

### Асинхронная отправка

Email отправляется в **фоновом потоке**, поэтому:

- ✅ Пользователь видит результат сразу
- ✅ Не ждет отправки письма
- ✅ Если ошибка - система продолжит работу

```python
def send_async_email(app, msg):
    """Отправляет в отдельном потоке"""
    with app.app_context():
        mail.send(msg)

thread = threading.Thread(
    target=send_async_email,
    args=(current_app._get_current_object(), msg)
)
thread.start()
```

## 🎨 HTML оформление писем

Все письма отправляются с красивым HTML оформлением:

- 📘 Профессиональный дизайн
- 🎨 Цветовое кодирование (желтое для очереди, зеленое для одобрения)
- 📱 Мобильная адаптивность
- 🇷🇺 Полностью на русском языке

## 💡 Полезные команды

```bash
# Проверить конфигурацию email
python test_email.py

# Отправить тестовое письмо
python test_email.py --send

# Просмотреть Flask-Mail версию
pip show Flask-Mail

# Переустановить Flask-Mail
pip install --upgrade Flask-Mail

# Проверить требуемые пакеты
pip list | grep -i "mail\|flask"
```

## 🔐 Безопасность

✅ Пароли хранятся в `.env` (не в коде)
✅ `.env` не должен быть в git (добавлен в `.gitignore`)
✅ Используется 16-символный App Password вместо обычного пароля
✅ TLS шифрование для подключения

## 📊 Статистика

- **Новые функции:** 3 функции отправки email
- **Обновленные файлы:** 5 файлов
- **Новые файлы:** 5 файлов
- **Строк кода:** ~300 строк в app/email.py
- **Размер Flask-Mail:** 8.5 KB

## 🎯 Что дальше?

Возможные улучшения:

- [ ] Отправка email при отмене бронирования (оригинальному забронировавшему)
- [ ] Автоматическое создание бронирования для первого в очереди
- [ ] Email подтверждение регистрации
- [ ] Email напоминания о предстоящем бронировании
- [ ] Batch отправка писем ночью
- [ ] Телеграм уведомления (дополнительно)

## ✅ Чек-лист готовности

- [ ] Установлена Flask-Mail (`pip install Flask-Mail`)
- [ ] Заполнен `.env` файл с MAIL_USERNAME и MAIL_PASSWORD
- [ ] Запущен `python test_email.py` и показал ✓ конфигурация OK
- [ ] Запущен `python test_email.py --send` и тестовое письмо пришло
- [ ] Сервер запущен: `python run.py`
- [ ] Прочитана полная документация в EMAIL_SETUP_GUIDE.md

---

**Система готова отправлять email-уведомления!** 🚀📧
